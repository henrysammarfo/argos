# Local dev — talks to FastAPI on port 8000
VITE_API_BASE_URL=http://localhost:8000/api

# Vercel production — DO NOT set the Azure IP here (causes mixed-content errors).
# Use /api (proxied to backend via vercel.json). See .env.production.
