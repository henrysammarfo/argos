{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "tanstack-start",
  "installCommand": "bun install --frozen-lockfile",
  "buildCommand": "bun run build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "http://51.103.219.168:8000/api/:path*"
    }
  ]
}
